# Create a function with arguments.
sumFunction <- function(a = 0, b = 0) {
   result <- a + b
   print(result)
}

# Call the function without giving any argument.
sumFunction()

# Call the function with giving new values of the argument.
sumFunction(9,5)


