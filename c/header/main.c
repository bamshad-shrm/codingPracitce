#include <stdio.h>
#include "math.h"  /* Include the header here, to obtain the function declaration */

int main(void) {
    int result = add(5,4);  /* Use the function here */
    printf("%d\n", result);
    return 0;
}
