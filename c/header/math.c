#include "math.h"  /* Include the header (not strictly necessary here) */

/* Function definition */
int add(int firstNumber, int secondNumber) {    
	int sum = firstNumber + secondNumber;
	return sum; 
}
